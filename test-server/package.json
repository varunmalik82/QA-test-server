{
  "name": "postman-test",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "windows": "set API_PORT=3000 && set NODE_ENV=dev && set TOKEN_KEY=cantGuessThisKey && nodemon server.js",
    "mac": "API_PORT=3000 NODE_ENV=dev TOKEN_KEY=cantGuessThisKey nodemon server.js"

  },
  "keywords": [],
  "dependencies": {
    "bcrypt": "^4.0.1",
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "jsonwebtoken": "^8.5.1",
    "nodemon": "^2.0.3"
  }
}